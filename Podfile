platform :ios, '9.0'

target 'TravelExpense' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

    pod 'Firebase/Core'
    pod 'Google-Mobile-Ads-SDK'
    pod 'SDCAlertView', '~> 7.1'
    pod 'PureLayout'

end
